# WP1's collection-only harness consumes no environment variables.
#
# WP2 will define placeholder-only variables after an approved staging fixture,
# authentication flow, and privacy-safe evidence contract have been designed.
# Never put credentials, tenant names, page IDs, cloud IDs, or board bodies here.
