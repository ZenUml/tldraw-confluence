# The checked-in local synthetic suite consumes no environment variables.
# Real Forge execution requires an approved fixture and authenticated browser path;
# those values stay in authorized private configuration, never this public file.
